# shell
A shell to run simple Linux commands

To compile and run,
```
gcc my_shell.c -o my_shell
./my_shell
```

Try executing simple commands such as,
```
$ ls
$ cat my_shell.c
$ echo "Hello World"
$ sleep 60
```
